@model IEnumerable<Teacher.Models.Student>

@foreach (Teacher.Models.Student Student in Model)
{
    <div>
        <a href="/StudentPage/Show/@Student.StudentId">@Student.StudentFirstName @Student.StudentLastName</a>
    </div>
}